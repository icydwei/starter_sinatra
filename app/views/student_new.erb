<h2>Create a New Student Account </h2>

<form action="/students" method="post">
        <label for="name">Full Name: </label>
        <input type="text" name="name">
    <br>
    <p>Enroll new student in courses by selecting below: </p>
        
        <% @courses.each do |course| %>
            <input type="checkbox" name="course_ids[]" id="<%=course.id%>" value="<%=course.id%>"><%= course.name %><br></input>
        <% end %>

        <input type="submit" value="Create Student Account!">
</form>
<br>
        